# Config for Qwen/Qwen3.5-9B (single GPU, 64GB VRAM)
# Model arguments
model_name_or_path: /public/home/act7jrkie6/SothisAI/model/Aihub/Qwen3.5-9B-Base/main/Qwen3.5-9B-Base
model_revision: main
torch_dtype: bf16
attn_implementation: flash_attention_2

# Data training arguments
# 这里沿用你仓库现有 SFT 模板风格；如不需要长 system prompt，可后续精简
chat_template: "{%- if tools %}\n    {{- '<|im_start|>system\\n' }}\n    {%- if messages[0]['role'] == 'system' %}\n        {{- messages[0]['content'] }}\n    {%- else %}\n        {{- 'You are Open-R1, a helpful assistant.' }}\n    {%- endif %}\n    {{- \"\\n\\n# Tools\\n\\nYou may call one or more functions to assist with the user query.\\n\\nYou are provided with function signatures within <tools></tools> XML tags:\\n<tools>\" }}\n    {%- for tool in tools %}\n        {{- \"\\n\" }}\n        {{- tool | tojson }}\n    {%- endfor %}\n    {{- \"\\n</tools>\\n\\nFor each function call, return a json object with function name and arguments within <tool_call></tool_call> XML tags:\\n<tool_call>\\n{\\\"name\\\": <function-name>, \\\"arguments\\\": <args-json-object>}\\n</tool_call><|im_end|>\\n\" }}\n{%- else %}\n    {%- if messages[0]['role'] == 'system' %}\n        {{- '<|im_start|>system\\n' + messages[0]['content'] + '<|im_end|>\\n' }}\n    {%- else %}\n        {{- '<|im_start|>system\\nYou are Open-R1, a helpful assistant.<|im_end|>\\n' }}\n    {%- endif %}\n{%- endif %}\n{%- for message in messages %}\n    {%- if (message.role == \"user\") or (message.role == \"system\" and not loop.first) or (message.role == \"assistant\" and not message.tool_calls) %}\n        {{- '<|im_start|>' + message.role + '\\n' + message.content + '<|im_end|>' + '\\n' }}\n    {%- elif message.role == \"assistant\" %}\n        {{- '<|im_start|>' + message.role }}\n        {%- if message.content %}\n            {{- '\\n' + message.content }}\n        {%- endif %}\n        {%- for tool_call in message.tool_calls %}\n            {%- if tool_call.function is defined %}\n                {%- set tool_call = tool_call.function %}\n            {%- endif %}\n            {{- '\\n<tool_call>\\n{\"name\": \"' }}\n            {{- tool_call.name }}\n            {{- '\", \"arguments\": ' }}\n            {{- tool_call.arguments | tojson }}\n            {{- '}\\n</tool_call>' }}\n        {%- endfor %}\n        {{- '<|im_end|>\\n' }}\n    {%- elif message.role == \"tool\" %}\n        {%- if (loop.index0 == 0) or (messages[loop.index0 - 1].role != \"tool\") %}\n            {{- '<|im_start|>user' }}\n        {%- endif %}\n        {{- '\\n<tool_response>\\n' }}\n        {{- message.content }}\n        {{- '\\n</tool_response>' }}\n        {%- if loop.last or (messages[loop.index0 + 1].role != \"tool\") %}\n            {{- '<|im_end|>\\n' }}\n        {%- endif %}\n    {%- endif %}\n{%- endfor %}\n{%- if add_generation_prompt %}\n    {{- '<|im_start|>assistant\\n' }}\n{%- endif %}\n"

dataset_name: /public/home/act7jrkie6/SothisAI/dataset/Console/data/main/data
dataset_config: all
dataset_num_proc: 4
eos_token: <|im_end|>

# SFT trainer config (single-card friendly)
bf16: true
do_eval: false
eval_strategy: "no"

# 显存核心策略：小 batch + 梯度累积
per_device_train_batch_size: 1
gradient_accumulation_steps: 16

# 稳定性与吞吐折中
gradient_checkpointing: true
gradient_checkpointing_kwargs:
  use_reentrant: false
max_length: 2048
packing: false

learning_rate: 1.0e-5
num_train_epochs: 3
warmup_ratio: 0.03
max_grad_norm: 0.3
weight_decay: 0.01

logging_steps: 10
logging_strategy: steps
save_strategy: epoch
save_total_limit: 2
seed: 42

output_dir: model/Qwen3.5-9B-Instruct-sft
overwrite_output_dir: true
push_to_hub: false
report_to:
  - wandb

# 可选优化开关（按环境支持开启）
use_liger_kernel: false
