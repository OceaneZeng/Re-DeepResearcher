# Qwen3.5-4B SFT on a single consumer GPU (e.g. RTX 3090 24GB) with Unsloth + 4bit LoRA
# Dataset: curated SFT parquet files under `data/subsets/sft/` (recursive glob)
#
# Usage (from repo root, single GPU):
#   accelerate launch --config_file recipes/accelerate_configs/ddp.yaml src/rl/sft.py \
#     --config recipes/Qwen3.5-4B/sft/config_unsloth_cuda.yaml
# Use two tokens after --config, or `--config=...` (sft.py normalizes this before TrlParser).
#
# Notes:
# - `nvidia-smi` "CUDA Version" is the *driver* capability; match your PyTorch build to an installed CUDA runtime.
# - For offline base weights, place a snapshot under `model/Qwen3.5-4B` and set `model_name_or_path` / `hf_local_dir` below.

# -----------------
# ScriptArguments
# -----------------
chat_template: |
  {%- if messages and messages[0]['role'] == 'system' -%}
  {{- '<|im_start|>system\n' + messages[0]['content'] + '<|im_end|>\n' -}}
  {%- else -%}
  {{- '<|im_start|>system\nYou are CARL-MoA Assistant. Think in modular agentic style (solver -> critic -> refiner, and tool calls when needed), then answer in strict tags: <think>...</think> followed by <answer>...</answer>.<|im_end|>\n' -}}
  {%- endif -%}
  {%- for message in messages -%}
    {%- if message['role'] != 'system' -%}
  {{- '<|im_start|>' + message['role'] + '\n' + message['content'] + '<|im_end|>\n' -}}
    {%- endif -%}
  {%- endfor -%}
  {%- if add_generation_prompt -%}
  {{- '<|im_start|>assistant\n' -}}
  {%- endif -%}
# Recommended layout:
#   data/subsets/sft/math/*.parquet
#   data/subsets/sft/logic/*.parquet
#   data/subsets/sft/code/*.parquet
# NOTE: Multiple files are fine; no need to merge into one file, but schema should be consistent.
dataset_name: data/subsets/sft/gspo_sft_unified_v2.parquet
allow_mixed_parquet_schemas: false
eos_token: <|im_end|>

# -----------------
# SFTConfig (training args)
# -----------------
output_dir: /home/featurize/work/model/Qwen3.5-4B-sft-unsloth
num_train_epochs: 2
# RTX 3090 24GB: 4bit + grad checkpointing + small micro-batch
per_device_train_batch_size: 1
gradient_accumulation_steps: 8
learning_rate: 1.0e-4
lr_scheduler_type: cosine
warmup_steps: 120
logging_steps: 5
save_steps: 300
save_total_limit: 2
report_to: ["wandb"]
wandb_project: Re-DeepResearcher
# run_name: qwen3.5-4b-sft-gspo-coldstart-v1
# wandb_entity: your_wandb_entity
# run_name: qwen3.5-4b-sft-unsloth

max_seq_length: 2048
packing: false

bf16: true
fp16: false
gradient_checkpointing: true
optim: paged_adamw_8bit

# -----------------
# Unsloth knobs
# -----------------
use_unsloth: true
unsloth_load_in_4bit: true
unsloth_local_files_only: false
unsloth_lora_r: 16
unsloth_lora_alpha: 32
unsloth_lora_dropout: 0.0
# unsloth_lora_target_modules: ["q_proj","k_proj","v_proj","o_proj","gate_proj","up_proj","down_proj"]

# -----------------
# ModelConfig (trl.ModelConfig)
# -----------------
# Optional: if you have a local base snapshot, set to that folder; otherwise rely on Hub cache
# hf_local_dir: model/Qwen3.5-4B
model_name_or_path: Qwen/Qwen3.5-4B
trust_remote_code: true
